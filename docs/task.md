# gosqlfmt 残タスク

## v0.1.0 リリースまで

### CLI実装
- [ ] `cmd/gosqlfmt/main.go` の実装
  - [ ] フラグ解析（`-w`, `-d`, `-l`）
  - [ ] ファイルパス指定でのフォーマット
  - [ ] ディレクトリ指定での再帰的フォーマット（`.go` ファイル対象）
  - [ ] 標準入力からの読み取り（パイプ対応）
  - [ ] デフォルト: フォーマット結果を標準出力に出力
  - [ ] `-w`: ファイル上書き
  - [ ] `-d`: unified diff表示
  - [ ] `-l`: フォーマットが必要なファイルパス一覧表示
  - [ ] 終了コード（フォーマット不要=0, フォーマット必要=1）
- [ ] CLIのテスト

### テスト強化
- [ ] 既にフォーマット済みのSQL（複数行）が再フォーマットで壊れないことの確認（冪等性テスト）
- [ ] 複数のSQL文字列を含むGoファイルのテスト
- [ ] エッジケース: 空のバッククォート文字列、改行を含むSQL、ネストされた括弧

### ドキュメント
- [ ] README.mdのフォーマット例をIntelliJプラグイン相当のスタイルに最終確認

---

## v0.2.0 以降

### フォーマット品質向上
- [ ] サブクエリ対応（括弧内のSELECTをネストインデント）
- [ ] CASE WHEN / THEN / ELSE / END のフォーマット
- [ ] SQLコメント（`--`, `/* */`）の保持
- [ ] BETWEEN ... AND の特殊処理（ANDで改行しない）
- [ ] 関数呼び出し内のカンマで改行しない制御の強化

### 設定ファイル対応
- [ ] `.gosqlfmt.yaml` の読み込み
- [ ] インデント幅のカスタマイズ（2/4スペース、タブ）
- [ ] キーワードケース選択（upper/lower/preserve）
- [ ] 複数行展開の閾値カスタマイズ
- [ ] カンマ位置（trailing/leading）

### マルチダイアレクト対応
- [ ] `--dialect` フラグの追加
- [ ] PostgreSQL固有構文（`::` キャスト、`RETURNING`、`ILIKE`）
- [ ] SQLite固有構文

### エディタ統合
- [ ] VSCode拡張
- [ ] GoLandプラグイン
- [ ] `goimports` のようなセーブ時自動実行
