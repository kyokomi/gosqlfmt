# gosqlfmt 残タスク

## v0.1.0 リリースまで

### CLI実装
- [x] `cmd/gosqlfmt/main.go` の実装
  - [x] フラグ解析（`-w`, `-d`, `-l`）
  - [x] ファイルパス指定でのフォーマット
  - [x] ディレクトリ指定での再帰的フォーマット（`.go` ファイル対象）
  - [x] 標準入力からの読み取り（パイプ対応）
  - [x] デフォルト: フォーマット結果を標準出力に出力
  - [x] `-w`: ファイル上書き
  - [x] `-d`: unified diff表示
  - [x] `-l`: フォーマットが必要なファイルパス一覧表示
  - [x] 終了コード（フォーマット不要=0, フォーマット必要=1）
- [x] CLIのテスト

### テスト強化
- [x] 既にフォーマット済みのSQL（複数行）が再フォーマットで壊れないことの確認（冪等性テスト）
- [x] 複数のSQL文字列を含むGoファイルのテスト
- [x] エッジケース: 空のバッククォート文字列、改行を含むSQL、ネストされた括弧

### ドキュメント
- [x] README.mdのフォーマット例をIntelliJプラグイン相当のスタイルに最終確認

---

## v0.2.0 フォーマット品質向上

### フォーマット品質向上
- [x] BETWEEN ... AND の特殊処理（ANDで改行しない）
- [x] 関数呼び出し内のカンマで改行しない制御の強化（テスト追加で検証）
- [x] CASE WHEN / THEN / ELSE / END のフォーマット
- [x] SQLコメント（`--`, `/* */`）の保持
- [x] サブクエリ対応（括弧内のSELECTをネストインデント、再帰深度制限5）

---

## v0.3.0 以降

### フォーマット品質のさらなる向上
- [ ] CTE (WITH句) 対応（`WITH cte AS (SELECT ...) SELECT ...`）
- [ ] ウィンドウ関数対応（`ROW_NUMBER() OVER (PARTITION BY ... ORDER BY ...)`）
- [ ] UNION / UNION ALL の改善（前後のSQLをそれぞれ独立して再帰整形）
- [ ] 複数文対応（セミコロン区切り）

### 設定ファイル対応
- [ ] `.gosqlfmt.yaml` の読み込み
- [ ] インデント幅のカスタマイズ（2/4スペース、タブ）
- [ ] キーワードケース選択（upper/lower/preserve）
- [ ] 複数行展開の閾値カスタマイズ
- [ ] カンマ位置（trailing/leading）

### マルチダイアレクト対応
- [ ] `--dialect` フラグの追加
- [ ] PostgreSQL固有構文（`::` キャスト、`RETURNING`、`ILIKE`）
- [ ] SQLite固有構文

### エディタ統合
- [ ] VSCode拡張
- [ ] GoLandプラグイン
- [ ] `goimports` のようなセーブ時自動実行

### テスト・品質
- [ ] 実プロジェクトでの検証（大きめのGoプロジェクトに適用して壊れないか確認）
- [ ] ベンチマーク（大量ファイル処理時のパフォーマンス計測）
- [ ] ファズテスト（異常な入力でパニックしないかの検証）
